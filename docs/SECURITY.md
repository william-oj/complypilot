# Security

- JWT auth with RBAC per organization
- OAuth (Google and Microsoft) via ID token verification
- Rate limiting and HTTP security headers
- Input validation using Zod
- Optional encryption for sensitive fields
- Evidence stored in S3-compatible storage
- Audit logging for critical actions
- Separate worker process for async tasks
